// This package implements the routing layer.
// Client can connect to this layer, authenticate and make service calls.
package router

// Represents the interface you connect to
// and issue service requests.
type Router struct {
}
